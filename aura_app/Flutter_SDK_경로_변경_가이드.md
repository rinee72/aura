# Flutter SDK 경로 변경 가이드 (한글 경로 문제 해결)

## 🎯 목적

한글 경로(`C:\Users\박인희\flutter`)에서 발생하는 shader 컴파일 오류를 해결하기 위해 Flutter SDK를 한글이 없는 경로로 이동합니다.

---

## 📋 사전 준비

### 1. 현재 Flutter 경로 확인

```powershell
flutter --version
```

출력 예시:
```
Flutter version 3.38.5 on channel stable at C:\Users\박인희\flutter
```

### 2. 새 경로 선택

**권장 경로**:
- `C:\flutter` (가장 간단)
- `C:\tools\flutter`
- `C:\dev\flutter`
- `D:\flutter` (다른 드라이브)

**피해야 할 경로**:
- ❌ `C:\Users\박인희\...` (한글 포함)
- ❌ `C:\Users\사용자명\...` (한글 포함)

---

## 🔧 단계별 실행 가이드

### 1단계: Flutter SDK 복사

```powershell
# 관리자 권한으로 PowerShell 실행 (Win + X → Windows PowerShell (관리자))

# 현재 Flutter 경로 확인
$oldPath = "C:\Users\박인희\flutter"
$newPath = "C:\flutter"

# 새 경로에 복사 (시간이 걸릴 수 있음)
Copy-Item -Path $oldPath -Destination $newPath -Recurse -Force

# 복사 확인
Test-Path "$newPath\bin\flutter.bat"
```

**예상 시간**: 5-10분 (SDK 크기에 따라 다름)

---

### 2단계: 환경 변수 업데이트

#### 방법 A: GUI를 통한 수정 (권장)

1. **시스템 속성 열기**:
   - `Win + X` → 시스템
   - 또는 `Win + R` → `sysdm.cpl` 입력

2. **환경 변수 편집**:
   - "고급" 탭 클릭
   - "환경 변수" 버튼 클릭

3. **Path 변수 수정**:
   - "시스템 변수" 또는 "사용자 변수"에서 `Path` 선택
   - "편집" 클릭
   - `C:\Users\박인희\flutter\bin` 찾기
   - "편집" 클릭하여 `C:\flutter\bin`으로 변경
   - 또는 "삭제" 후 "새로 만들기"로 `C:\flutter\bin` 추가

4. **확인**:
   - 모든 창에서 "확인" 클릭

#### 방법 B: PowerShell을 통한 수정

```powershell
# 관리자 권한으로 PowerShell 실행

# 현재 Path 확인
$currentPath = [Environment]::GetEnvironmentVariable("Path", "User")
Write-Host "현재 Path: $currentPath"

# 한글 경로 제거 및 새 경로 추가
$newPath = $currentPath -replace "C:\\Users\\박인희\\flutter\\bin", "C:\flutter\bin"
if ($newPath -notlike "*C:\flutter\bin*") {
    $newPath = "$newPath;C:\flutter\bin"
}

# 환경 변수 업데이트
[Environment]::SetEnvironmentVariable("Path", $newPath, "User")

# 확인
Write-Host "업데이트된 Path: $([Environment]::GetEnvironmentVariable("Path", "User"))"
```

---

### 3단계: 새 터미널에서 확인

**중요**: 환경 변수 변경 후 **새 터미널**을 열어야 합니다.

```powershell
# 새 PowerShell 창 열기

# Flutter 경로 확인
flutter --version

# 출력 예시:
# Flutter version 3.38.5 on channel stable at C:\flutter
# ✅ 한글 경로가 사라졌는지 확인

# Flutter Doctor 실행
flutter doctor
```

---

### 4단계: 프로젝트에서 테스트

```powershell
cd c:\modu\aura_app

# 클린 빌드
flutter clean

# 의존성 재설치
flutter pub get

# 웹 앱 실행 테스트
flutter run -d chrome --dart-define=ENVIRONMENT=development
```

**예상 결과**: Shader 컴파일 오류 없이 정상 실행

---

## ✅ 성공 확인

다음 조건이 모두 충족되면 성공:

1. ✅ `flutter --version`에서 새 경로 표시 (`C:\flutter`)
2. ✅ `flutter doctor` 정상 실행
3. ✅ `flutter run -d chrome` 실행 시 shader 컴파일 오류 없음
4. ✅ Chrome에서 앱이 정상적으로 열림

---

## 🔧 문제 해결

### 문제 1: "flutter: 명령을 찾을 수 없습니다"

**원인**: 환경 변수가 제대로 업데이트되지 않음

**해결책**:
1. 모든 터미널 창 닫기
2. 새 터미널 열기
3. `$env:Path` 확인
4. 필요시 컴퓨터 재시작

### 문제 2: 복사 중 오류 발생

**원인**: 권한 부족 또는 디스크 공간 부족

**해결책**:
1. 관리자 권한으로 PowerShell 실행
2. 디스크 공간 확인 (최소 2GB 필요)
3. 다른 드라이브 사용 고려

### 문제 3: 환경 변수 변경이 적용되지 않음

**해결책**:
1. 컴퓨터 재시작
2. 또는 로그아웃 후 다시 로그인

---

## 📝 추가 참고사항

### 기존 Flutter SDK 삭제 (선택사항)

경로 변경 후 기존 Flutter SDK를 삭제할 수 있습니다:

```powershell
# 주의: 새 경로에서 정상 작동하는지 확인한 후에만 실행
Remove-Item -Path "C:\Users\박인희\flutter" -Recurse -Force
```

**주의**: 삭제 전에 새 경로에서 Flutter가 정상 작동하는지 반드시 확인하세요!

---

## 🎉 완료!

Flutter SDK 경로 변경이 완료되면:
- ✅ Shader 컴파일 오류 해결
- ✅ 웹 앱 정상 실행
- ✅ Windows 앱 정상 실행
- ✅ 모든 플랫폼에서 정상 작동
