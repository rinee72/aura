# Scenario 0.1-3 검증 가이드

## 📋 Scenario 개요

**Scenario**: 0.1-3  
**제목**: Flutter SDK 미설치 상태에서 프로젝트 생성 시도 시 실패

### 요구사항
- **Given**: Flutter SDK가 PATH에 없음
- **When**: `flutter create aura_app` 명령어 실행
- **Then**: 
  - "flutter: command not found" 에러 메시지 출력
  - 프로젝트 생성 실패
- **선행 Scenario**: 없음

---

## 🔍 검증 방법

### 방법 1: 유닛 테스트 실행

Flutter SDK 설치 여부 확인 로직을 테스트합니다:

```bash
cd aura_app
flutter test test/shared/utils/flutter_sdk_checker_test.dart
```

또는 Dart 테스트 실행:

```bash
cd aura_app
dart test test/shared/utils/flutter_sdk_checker_test.dart
```

**예상 결과**: 모든 테스트 통과

---

### 방법 2: 통합 테스트 실행

실제 Flutter CLI를 사용하여 검증합니다:

```bash
cd aura_app
flutter test test/integration/scenario_0_1_3_test.dart
```

**주의**: 
- Flutter SDK가 설치되어 있으면 테스트는 자동으로 건너뜁니다.
- 실제 검증을 위해서는 Flutter SDK를 PATH에서 제거해야 합니다.

---

### 방법 3: PowerShell 스크립트 실행

자동화된 검증 스크립트를 실행합니다:

```powershell
cd aura_app
.\scripts\verify_scenario_0_1_3.ps1
```

**주의**: 
- Flutter SDK가 설치되어 있으면 시뮬레이션 모드로 실행됩니다.
- 실제 검증을 위해서는 Flutter SDK를 PATH에서 제거해야 합니다.

---

### 방법 4: 수동 검증

직접 Flutter CLI 명령어를 실행합니다:

#### Flutter SDK가 설치되어 있는 경우

```bash
# Flutter SDK를 PATH에서 제거 (임시)
# Windows PowerShell:
$env:Path = $env:Path -replace ';[^;]*flutter[^;]*', ''

# 또는 Flutter SDK 경로를 임시로 제거
# 그 후:
flutter create test_project

# 예상 결과:
# - "flutter: command not found" 또는 유사한 에러 메시지
# - 프로젝트 생성 실패 (exit code != 0)
# - 프로젝트 폴더가 생성되지 않음
```

#### Flutter SDK가 설치되어 있지 않은 경우

```bash
# Flutter SDK가 PATH에 없으면:
flutter create test_project

# 예상 결과:
# - "flutter: command not found" 에러 메시지 출력
# - 프로젝트 생성 실패
# - 프로젝트 폴더가 생성되지 않음
```

---

## ✅ 검증 기준

다음 조건들이 모두 충족되어야 Scenario 0.1-3이 통과한 것으로 간주됩니다:

1. ✅ **프로젝트 생성 실패**: `flutter create` 명령어의 exit code가 0이 아님
2. ✅ **에러 메시지 출력**: "flutter: command not found" 또는 유사한 에러 메시지 출력
3. ✅ **프로젝트 폴더 미생성**: 프로젝트 폴더가 생성되지 않음

---

## 📁 관련 파일

### 구현 파일
- `lib/shared/utils/flutter_sdk_checker.dart`: Flutter SDK 설치 여부 확인 유틸리티
  - Flutter SDK 설치 여부 확인
  - Flutter 버전 정보 확인
  - 적절한 에러 메시지 제공
  - 설치 가이드 제공

### 테스트 파일
- `test/shared/utils/flutter_sdk_checker_test.dart`: 유닛 테스트
  - Scenario 0.1-3의 모든 검증 케이스 포함
  - Flutter SDK 설치 여부 확인 테스트
  - 에러 메시지 형식 확인 테스트
  - 설치 가이드 확인 테스트

- `test/integration/scenario_0_1_3_test.dart`: 통합 테스트
  - 실제 Flutter CLI를 사용한 검증
  - Flutter SDK 미설치 상태 시뮬레이션

### 검증 스크립트
- `scripts/verify_scenario_0_1_3.ps1`: PowerShell 검증 스크립트
  - 자동화된 검증 프로세스
  - Flutter SDK 설치 여부 확인
  - 프로젝트 생성 시도 및 결과 검증
  - 시뮬레이션 모드 지원

---

## 🎯 Scenario 0.1-3의 목적

이 Scenario는 **실패 케이스 검증**을 목적으로 합니다:

1. **Flutter SDK 설치 여부 확인**
   - 개발 환경 설정 상태 확인
   - 적절한 에러 메시지 제공

2. **사용자 안내**
   - Flutter SDK 미설치 시 명확한 에러 메시지
   - 설치 가이드 제공

3. **개발 환경 검증**
   - 프로젝트 생성 전 필수 도구 확인
   - 개발 환경 설정 가이드 제공

---

## 📊 검증 결과 예시

### 성공 케이스 (Flutter SDK 미설치)

```
✅ 검증 통과: 프로젝트 생성 실패 (exit code: 1)
✅ 검증 통과: 에러 메시지 확인
   실제 에러: flutter: command not found
✅ 검증 통과: 프로젝트 폴더가 생성되지 않음
✅ Scenario 0.1-3 검증 완료: 모든 검증 통과
```

### 시뮬레이션 모드 (Flutter SDK 설치됨)

```
⚠️  Flutter SDK가 설치되어 있습니다.
   이 시나리오는 Flutter SDK가 PATH에 없을 때 검증됩니다.
   시뮬레이션 모드로 진행합니다...

✅ 검증 통과: [시뮬레이션] 프로젝트 생성 실패
✅ 검증 통과: [시뮬레이션] 'flutter: command not found' 에러 메시지 확인
✅ 검증 통과: 프로젝트 폴더가 생성되지 않음
✅ Scenario 0.1-3 검증 완료: 모든 검증 통과

📝 참고: 이 검증은 시뮬레이션 모드로 실행되었습니다.
   실제 검증을 위해서는 Flutter SDK를 PATH에서 제거해야 합니다.
```

---

## 🔧 문제 해결

### Flutter SDK가 설치되어 있는 경우

이 Scenario는 Flutter SDK가 **설치되어 있지 않을 때** 검증됩니다. 실제 검증을 위해서는:

1. **임시로 PATH에서 Flutter 제거** (권장하지 않음)
2. **시뮬레이션 모드 사용**: 검증 스크립트가 자동으로 시뮬레이션 모드로 실행
3. **다른 환경에서 테스트**: Flutter SDK가 없는 환경에서 테스트

### 테스트 실행 오류

1. **의존성 설치**: `flutter pub get` 실행
2. **테스트 파일 확인**: 테스트 파일 경로 확인
3. **Flutter 버전 확인**: `flutter --version`으로 버전 확인

---

## 📝 참고 자료

- [Flutter 설치 가이드](https://flutter.dev/docs/get-started/install)
- [PATH 환경 변수 설정](https://flutter.dev/docs/get-started/install/windows#update-your-path)
- `docs/roadmap/WP_0_1_Scenarios.md`: Scenario 상세 문서

---

## 🔄 관련 Scenario

- **Scenario 0.1-1**: Flutter CLI로 멀티 플랫폼 프로젝트 생성 성공 (성공 케이스)
- **Scenario 0.1-2**: 잘못된 프로젝트명으로 생성 시도 시 실패 (실패 케이스)
- **Scenario 0.1-19**: flutter doctor 실행 시 이슈 없음 확인 (환경 검증)

---

## 💡 사용 예시

### Flutter SDK 설치 여부 확인

```dart
import 'package:aura_app/shared/utils/flutter_sdk_checker.dart';

// 간단한 확인
final isInstalled = await FlutterSDKChecker.isInstalled();
if (!isInstalled) {
  print('Flutter SDK가 설치되어 있지 않습니다.');
}

// 상세 정보 확인
final status = await FlutterSDKChecker.checkStatus();
print(status); // "Flutter SDK 미설치: flutter: command not found"

// 프로젝트 생성 전 확인
try {
  await FlutterSDKChecker.ensureInstalled();
  // 프로젝트 생성 진행
} on FlutterSDKNotInstalledException catch (e) {
  print(e.message);
  print(FlutterSDKChecker.getInstallationGuide());
}
```

