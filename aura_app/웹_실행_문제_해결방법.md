# Flutter 웹 실행 문제 및 해결 방법

## 🔴 발견된 문제

### 1. Shader 컴파일 오류
```
ShaderCompilerException: Shader compilation failed
```
**원인**: 한글 경로(`C:\Users\박인희\...`)에서 Flutter의 shader 컴파일러가 실패

### 2. Symlink 지원 필요
```
Building with plugins requires symlink support.
Please enable Developer Mode in your system settings.
```
**원인**: Windows에서 symlink를 사용하려면 Developer Mode가 필요

## ✅ 해결 방법

### 방법 1: Windows Developer Mode 활성화 (권장)

1. **설정 열기**:
   - `Win + I` 키를 눌러 설정 열기
   - 또는 `start ms-settings:developers` 명령 실행

2. **Developer Mode 활성화**:
   - 설정 > 개인 정보 보호 및 보안 > 개발자용
   - "개발자 모드" 토글을 **켜기**

3. **재부팅** (필요시)

4. **다시 실행**:
   ```powershell
   cd c:\modu\aura_app
   flutter run -d chrome --dart-define=ENVIRONMENT=development
   ```

### 방법 2: Windows Desktop 앱으로 확인

웹 대신 Windows Desktop 앱으로 실행:
```powershell
cd c:\modu\aura_app
flutter run -d windows --dart-define=ENVIRONMENT=development
```

**장점**:
- Symlink 문제 없음
- 한글 경로 문제 영향 적음
- 네이티브 성능

### 방법 3: Flutter 경로 변경 (근본 해결)

Flutter SDK를 한글이 없는 경로로 이동:
1. Flutter SDK를 `C:\flutter`로 이동
2. 환경 변수 `PATH` 업데이트
3. `flutter doctor` 실행하여 확인

## 📝 현재 시도 중

Windows Desktop 앱으로 실행 중입니다. 이 방법은 symlink 문제를 우회할 수 있습니다.

## 🔍 확인할 내용

Windows Desktop 앱이 실행되면:
1. 홈 화면에서 AppBar 오른쪽의 **팔레트 아이콘(🎨)** 클릭
2. 컴포넌트 카탈로그에서 디자인 시스템 확인:
   - Colors
   - Typography
   - Spacing
   - Buttons
   - Text Fields
   - Cards
   - Loading
   - Error
