# WP-0.1 완료 체크리스트

## ✅ 완료된 작업

### 1. Flutter 프로젝트 생성
- [x] `aura_app` 폴더 생성
- [x] `pubspec.yaml` 파일 생성
- [x] `lib/main.dart` 파일 생성
- [x] 기본 Flutter 앱 구조 완성

### 2. 필수 의존성 추가
- [x] `supabase_flutter: ^2.3.0` 추가
- [x] `go_router: ^13.0.0` 추가
- [x] `provider: ^6.1.1` 추가
- [x] `flutter_dotenv: ^5.1.0` 추가
- [x] `flutter_lints: ^3.0.0` (dev) 추가

### 3. iOS 플랫폼 설정
- [x] `ios/Runner/Info.plist` 생성
- [x] 카메라 권한 추가 (`NSCameraUsageDescription`)
- [x] 갤러리 권한 추가 (`NSPhotoLibraryUsageDescription`)
- [x] `ios/Runner/AppDelegate.swift` 생성
- [x] `ios/Runner.xcodeproj/project.pbxproj` 생성

### 4. Android 플랫폼 설정
- [x] `android/app/build.gradle` 생성
- [x] `minSdkVersion 21` 설정 완료
- [x] `android/app/src/main/AndroidManifest.xml` 생성
- [x] 인터넷 권한 추가 (`INTERNET`)
- [x] `android/app/src/main/kotlin/com/aura/aura_app/MainActivity.kt` 생성
- [x] Android 빌드 설정 파일들 생성

### 5. Web 플랫폼 설정
- [x] `web/index.html` 생성
- [x] 메타 태그 추가 (description, viewport)
- [x] `web/manifest.json` 생성

### 6. 프로젝트 구조
- [x] `lib/core/` 폴더 생성
- [x] `lib/features/` 폴더 생성
- [x] `lib/shared/` 폴더 생성
- [x] `lib/shared/widgets/` 폴더 생성
- [x] `lib/shared/utils/` 폴더 생성
- [x] `test/` 폴더 생성
- [x] `assets/` 폴더 생성

### 7. 기타 파일
- [x] `README.md` 생성
- [x] `.gitignore` 생성
- [x] `analysis_options.yaml` 생성
- [x] `.env.example` 생성

## 📋 다음 단계 (WP-0.2)

다음 작업을 진행하려면:
1. Flutter SDK 설치 확인: `flutter doctor`
2. 의존성 설치: `flutter pub get`
3. Supabase 프로젝트 생성 및 연결 (WP-0.2)

## 🎯 WP-0.1 완료 조건 검증

### 필수 통과 시나리오
- ✅ 0.1-1: 프로젝트 생성 성공 (수동 생성 완료)
- ✅ 0.1-4: 의존성 설치 준비 완료 (pubspec.yaml 설정)
- ✅ 0.1-7: iOS 프로젝트 기본 설정 완료
- ✅ 0.1-10: Android Minimum SDK 21 설정 완료
- ✅ 0.1-11: 인터넷 권한 추가 완료
- ✅ 0.1-13: Web 프로젝트 기본 설정 완료
- ✅ 0.1-14: index.html 메타 태그 추가 완료

### 실행 테스트 필요 (Flutter SDK 설치 후)
- [ ] 0.1-15: Chrome에서 앱 실행 성공
- [ ] 0.1-16: iOS 시뮬레이터에서 앱 실행 성공
- [ ] 0.1-17: Android 에뮬레이터에서 앱 실행 성공
- [ ] 0.1-19: flutter doctor 실행 시 이슈 없음 확인

## 📝 참고사항

- Flutter SDK가 설치되어 있지 않은 경우, 프로젝트 구조는 완성되었지만 실제 실행은 Flutter SDK 설치 후 가능합니다.
- 모든 플랫폼별 설정 파일이 준비되어 있어, Flutter SDK 설치 후 즉시 `flutter pub get` 및 `flutter run` 명령으로 실행할 수 있습니다.

